# Project 04: Network Diagnosis Playbook

Write a playbook for a service connectivity failure.

Cover:

- DNS resolution
- route table
- local listener
- firewall/security group
- TCP connectivity
- TLS certificate/SNI
- HTTP status
- application logs

Deliverable:

```text
DNS -> route -> listener -> firewall -> TLS -> HTTP -> app logs
```