# Project 03: Log Triage Pipeline

Build a command pipeline that:

- searches for errors
- extracts useful fields
- counts repeated messages
- sorts by frequency
- summarizes top causes

Example tools:

```bash
grep
awk
sed
sort
uniq
tail
wc
```

Discuss false positives, compressed logs, rotated logs, and noisy errors.