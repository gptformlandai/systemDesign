# Project 02: Service Debugging Playbook

Create a playbook for a failed service.

Include:

- `systemctl status`
- `journalctl -u`
- unit file inspection
- service user and working directory
- config validation
- port listener check
- rollback and reload plan

Deliverable:

```text
symptom -> evidence -> likely cause -> mitigation -> validation -> prevention
```